# 机制查看器 v10

第 **10** 篇配套：默认加载 `langgraph_v10`（LangGraph 图编排 + v8 工具表）。

```powershell
cd agent_lab/mechanism_viewer_v10
pip install -r requirements.txt
python mechanism_client.py
```

Windows 可双击 `启动机制查看器.bat`。
