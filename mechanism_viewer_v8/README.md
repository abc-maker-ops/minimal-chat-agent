# 机制查看器 v8

第 **09** 篇配套：默认加载 `react_v8`（tool_choice / parallel / JSON Schema 工程化）。

```powershell
cd agent_lab/mechanism_viewer_v8
pip install -r requirements.txt
python mechanism_client.py
```

Windows 可双击 `启动机制查看器.bat`。
