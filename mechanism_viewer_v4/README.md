# mechanism_viewer_v4

**机制查看器 v4** — 对应 **`role_setting_v4`**（系列第05篇）。

版本下拉**仅**：CoT + Few-shot / CoT Zero-shot / 简化 ToT + CoT。在 v3 之上增加 **推理与比选** 标签页。

```powershell
$env:ZHIPU_API_KEY="你的Key"
cd mechanism_viewer_v4
pip install -r requirements.txt
python mechanism_client.py
```
